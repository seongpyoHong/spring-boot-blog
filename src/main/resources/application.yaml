spring:
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true
  profiles:
    include: oauth
  redis:
    host: localhost
    port: 6379
    session:
      timeout: 600
      store-type: redis
      redis:
        flush-mode: on-save
        namespace: spring:session